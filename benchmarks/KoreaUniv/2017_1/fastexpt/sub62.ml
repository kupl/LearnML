(* your code goes here *)
(* problem 1*)
let fastexpt : int -> int -> int
= fun b n -> (let n = )
 if n = 0 then 1
 else if(n % 2 == 0) (x * x, n / 2);
 else return x * exponentiate(x * x, (n - 1) / 2);